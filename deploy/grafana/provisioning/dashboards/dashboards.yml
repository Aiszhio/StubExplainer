apiVersion: 1

providers:
  - name: Explainer dashboards
    orgId: 1
    folder: Explainer
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
